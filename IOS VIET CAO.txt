-- [[ 1. THIẾT LẬP HỆ THỐNG KEY & CẤU HÌNH ]] --
local MyKeys = {
    ["Tudeptrai98"] = 1, ["Tudeptrai88"] = 1, ["Tudeptrai10"] = 1, ["Tudeptrai100"] = 1,
    ["AdminVip99999"] = 99999
}
local MyLinkGetKey = "https://link-hub.net/4158616/x3aBaZwPB7Uy" 

local LP = game:GetService("Players").LocalPlayer
local Mouse = LP:GetMouse()
local Camera = game:GetService("Workspace").CurrentCamera
local Global_Green = Color3.fromRGB(0, 255, 127)

_G.Config = {
    Aimbot = false, ShowFOV = true, FOV_Radius = 150,
    ESP_Line = true, ESP_Box = true, Hitbox = false, Speed = false
}

-- [[ 2. VÒNG TRÒN FOV (DRAWING) ]] --
local FOV_Circle = Drawing.new("Circle")
FOV_Circle.Visible = _G.Config.ShowFOV
FOV_Circle.Radius = _G.Config.FOV_Radius
FOV_Circle.Color = Global_Green
FOV_Circle.Thickness = 2
FOV_Circle.Transparency = 1
FOV_Circle.NumSides = 64

-- [[ 3. HÀM TẠO ESP GIỐNG ẢNH ]] --
local function CreateESP(Player)
    local Line = Drawing.new("Line")
    local Box = Drawing.new("Square")
    local Info = Drawing.new("Text")

    game:GetService("RunService").RenderStepped:Connect(function()
        if Player.Character and Player.Character:FindFirstChild("HumanoidRootPart") and Player.Character:FindFirstChild("Humanoid") and Player.Parent ~= nil then
            local Pos, OnScreen = Camera:WorldToViewportPoint(Player.Character.HumanoidRootPart.Position)
            if OnScreen then
                -- ESP LINE (Từ đỉnh xuống - Giống ảnh)
                Line.Visible = _G.Config.ESP_Line
                Line.From = Vector2.new(Camera.ViewportSize.X / 2, 0)
                Line.To = Vector2.new(Pos.X, Pos.Y)
                Line.Color = Global_Green
                Line.Thickness = 1

                -- ESP BOX
                Box.Visible = _G.Config.ESP_Box
                Box.Size = Vector2.new(2000 / Pos.Z, 3000 / Pos.Z)
                Box.Position = Vector2.new(Pos.X - Box.Size.X / 2, Pos.Y - Box.Size.Y / 2)
                Box.Color = Global_Green

                -- INFO HP/NAME
                Info.Visible = _G.Config.ESP_Box
                Info.Text = Player.Name .. " | HP: " .. math.floor(Player.Character.Humanoid.Health)
                Info.Position = Vector2.new(Pos.X, Pos.Y + (Box.Size.Y / 2))
                Info.Center = true; Info.Size = 14; Info.Color = Color3.new(1,1,1)
            else
                Line.Visible = false; Box.Visible = false; Info.Visible = false
            end
        else
            Line.Visible = false; Box.Visible = false; Info.Visible = false
        end
    end)
end

-- [[ 4. LOGIC TÁC DỤNG THẬT (TC) ]] --
local function GetTarget()
    local Target = nil
    local Dist = math.huge
    for _, v in pairs(game:GetService("Players"):GetPlayers()) do
        if v ~= LP and v.Character and v.Character:FindFirstChild("HumanoidRootPart") then
            local Pos, OnScreen = Camera:WorldToViewportPoint(v.Character.HumanoidRootPart.Position)
            if OnScreen then
                local MouseDist = (Vector2.new(Pos.X, Pos.Y) - Vector2.new(Mouse.X, Mouse.Y)).Magnitude
                if MouseDist <= _G.Config.FOV_Radius and MouseDist < Dist then
                    Target = v; Dist = MouseDist
                end
            end
        end
    end
    return Target
end

game:GetService("RunService").RenderStepped:Connect(function()
    FOV_Circle.Visible = _G.Config.ShowFOV
    FOV_Circle.Radius = _G.Config.FOV_Radius
    FOV_Circle.Position = Vector2.new(Mouse.X, Mouse.Y + 36)

    if _G.Config.Aimbot then
        local T = GetTarget()
        if T then Camera.CFrame = CFrame.new(Camera.CFrame.Position, T.Character.HumanoidRootPart.Position) end
    end

    if _G.Config.Speed then LP.Character.Humanoid.WalkSpeed = 100 else LP.Character.Humanoid.WalkSpeed = 16 end

    for _, v in pairs(game:GetService("Players"):GetPlayers()) do
        if v ~= LP and v.Character and v.Character:FindFirstChild("HumanoidRootPart") then
            if _G.Config.Hitbox then
                v.Character.HumanoidRootPart.Size = Vector3.new(20,20,20)
                v.Character.HumanoidRootPart.Transparency = 0.7
            else
                v.Character.HumanoidRootPart.Size = Vector3.new(2,2,2)
                v.Character.HumanoidRootPart.Transparency = 1
            end
        end
    end
end)

-- Khởi tạo ESP cho người chơi hiện tại và mới vào
for _, v in pairs(game:GetService("Players"):GetPlayers()) do if v ~= LP then CreateESP(v) end end
game:GetService("Players").PlayerAdded:Connect(function(v) CreateESP(v) end)

-- [[ 5. GIAO DIỆN MENU CHÍNH ]] --
local function LaunchMenu(days)
    local Screen = Instance.new("ScreenGui", LP.PlayerGui)
    local Main = Instance.new("Frame", Screen)
    Main.Size = UDim2.new(0, 260, 0, 450); Main.Position = UDim2.new(0.5, -130, 0.5, -225)
    Main.BackgroundColor3 = Color3.fromRGB(15, 15, 15); Main.Active = true; Main.Draggable = true; Instance.new("UICorner", Main)

    local Title = Instance.new("TextLabel", Main)
    Title.Size = UDim2.new(1, 0, 0, 40); Title.Text = "TU MODER VIP"; Title.TextColor3 = Global_Green; Title.Font = "GothamBold"; Title.BackgroundTransparency = 1

    local Info = Instance.new("TextLabel", Main)
    Info.Size = UDim2.new(1, 0, 0, 30); Info.Position = UDim2.new(0, 0, 0, 35)
    Info.Text = "Chào: " .. LP.Name .. " | Hạn: " .. days .. " ngày"; Info.TextColor3 = Color3.new(1,1,1); Info.BackgroundTransparency = 1

    -- Nút Ẩn/Hiện
    local CloseBtn = Instance.new("TextButton", Main)
    CloseBtn.Size = UDim2.new(0, 25, 0, 25); CloseBtn.Position = UDim2.new(1, -30, 0, 5)
    CloseBtn.Text = "X"; CloseBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50); CloseBtn.TextColor3 = Color3.new(1,1,1); Instance.new("UICorner", CloseBtn)
    CloseBtn.MouseButton1Click:Connect(function()
        Main.Visible = false
        local OpenBtn = Instance.new("TextButton", Screen)
        OpenBtn.Size = UDim2.new(0, 45, 0, 45); OpenBtn.Position = UDim2.new(0, 10, 0.5, -22); OpenBtn.Text = "TU"; OpenBtn.BackgroundColor3 = Global_Green; Instance.new("UICorner", OpenBtn)
        OpenBtn.MouseButton1Click:Connect(function() Main.Visible = true; OpenBtn:Destroy() end)
    end)

    -- Chỉnh FOV (1 - 300)
    local FOVTxt = Instance.new("TextLabel", Main)
    FOVTxt.Size = UDim2.new(1, 0, 0, 30); FOVTxt.Position = UDim2.new(0, 0, 0, 70); FOVTxt.Text = "FOV SIZE: " .. _G.Config.FOV_Radius; FOVTxt.TextColor3 = Color3.new(1,1,1); FOVTxt.BackgroundTransparency = 1

    local Down = Instance.new("TextButton", Main); Down.Size = UDim2.new(0, 50, 0, 30); Down.Position = UDim2.new(0.5, -110, 0, 100); Down.Text = "-"; Down.BackgroundColor3 = Color3.fromRGB(40,40,40); Down.TextColor3 = Color3.new(1,1,1); Instance.new("UICorner", Down)
    local Up = Instance.new("TextButton", Main); Up.Size = UDim2.new(0, 50, 0, 30); Up.Position = UDim2.new(0.5, 60, 0, 100); Up.Text = "+"; Up.BackgroundColor3 = Color3.fromRGB(40,40,40); Up.TextColor3 = Color3.new(1,1,1); Instance.new("UICorner", Up)
    
    Down.MouseButton1Click:Connect(function() _G.Config.FOV_Radius = math.clamp(_G.Config.FOV_Radius - 10, 1, 300) FOVTxt.Text = "FOV SIZE: " .. _G.Config.FOV_Radius end)
    Up.MouseButton1Click:Connect(function() _G.Config.FOV_Radius = math.clamp(_G.Config.FOV_Radius + 10, 1, 300) FOVTxt.Text = "FOV SIZE: " .. _G.Config.FOV_Radius end)

    local function AddBtn(name, cfg, y)
        local b = Instance.new("TextButton", Main)
        b.Size = UDim2.new(0, 220, 0, 35); b.Position = UDim2.new(0.5, -110, 0, y)
        b.Text = name; b.BackgroundColor3 = Color3.fromRGB(30,30,30); b.TextColor3 = _G.Config[cfg] and Global_Green or Color3.new(1,1,1); Instance.new("UICorner", b)
        b.MouseButton1Click:Connect(function()
            _G.Config[cfg] = not _G.Config[cfg]
            b.TextColor3 = _G.Config[cfg] and Global_Green or Color3.new(1,1,1)
        end)
    end

    AddBtn("AIMBOT THEO FOV", "Aimbot", 145)
    AddBtn("HIỆN VÒNG FOV", "ShowFOV", 190)
    AddBtn("ESP LINE & BOX", "ESP_Line", 235)
    AddBtn("MOD HITBOX LARGE", "Hitbox", 280)
    AddBtn("SPEED HACK", "Speed", 325)
end

-- [[ 6. HỆ THỐNG KEY ]] --
local function CreateKeySystem()
    local KeyGui = Instance.new("ScreenGui", LP.PlayerGui)
    local F = Instance.new("Frame", KeyGui); F.Size = UDim2.new(0, 320, 0, 240); F.Position = UDim2.new(0.5, -160, 0.5, -120); F.BackgroundColor3 = Color3.fromRGB(20, 20, 20); Instance.new("UICorner", F)
    local T = Instance.new("TextLabel", F); T.Size = UDim2.new(1, 0, 0, 50); T.Text = "TU VIP MODER"; T.TextColor3 = Global_Green; T.Font = "GothamBold"; T.BackgroundTransparency = 1
    local Inp = Instance.new("TextBox", F); Inp.Size = UDim2.new(0, 260, 0, 45); Inp.Position = UDim2.new(0.5, -130, 0.35, 0); Inp.PlaceholderText = "Nhập Key..."; Inp.BackgroundColor3 = Color3.fromRGB(35, 35, 35); Inp.TextColor3 = Color3.new(1,1,1); Instance.new("UICorner", Inp)
    local GetB = Instance.new("TextButton", F); GetB.Size = UDim2.new(0, 125, 0, 45); GetB.Position = UDim2.new(0.5, -130, 0.7, 0); GetB.Text = "LẤY KEY"; GetB.BackgroundColor3 = Color3.fromRGB(200, 50, 50); GetB.TextColor3 = Color3.new(1,1,1); Instance.new("UICorner", GetB)
    GetB.MouseButton1Click:Connect(function() setclipboard(MyLink4m) GetB.Text = "ĐÃ COPY!" wait(1) GetB.Text = "LẤY KEY" end)
    local CheckB = Instance.new("TextButton", F); CheckB.Size = UDim2.new(0, 125, 0, 45); CheckB.Position = UDim2.new(0.5, 5, 0.7, 0); CheckB.Text = "NHẬP"; CheckB.BackgroundColor3 = Color3.fromRGB(0, 150, 255); CheckB.TextColor3 = Color3.new(1,1,1); Instance.new("UICorner", CheckB)
    CheckB.MouseButton1Click:Connect(function()
        local d = MyKeys[Inp.Text]
        if d then KeyGui:Destroy() LaunchMenu(d) else CheckB.Text = "SAI KEY!" wait(1) CheckB.Text = "NHẬP" end
    end)
end

CreateKeySystem()
